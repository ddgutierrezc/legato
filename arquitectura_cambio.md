# Arquitectura del cambio

> Historical note: some roadmap sections predate shipped iOS runtime milestones and remain preserved only as superseded planning history.

## Context

This file exists as a historical bridge for legacy documentation/tests that still verify old roadmap wording is explicitly marked as superseded.

## Current authority

- Canonical public product documentation lives under `apps/docs-site/src/content/docs/`.
- Maintainer-only architecture and release governance live under `docs/maintainers/` and `docs/releases/`.
- Legacy roadmap notes must not be treated as the current source of truth for runtime support claims.
